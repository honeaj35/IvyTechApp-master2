<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="net.androidbootcamp.ivytechapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <permission
        android:name="com.example.ivytechapp.permission.MAPS_RECEIVE"
        android:protectionLevel="signature"/>
    <uses-permission android:name="com.example.ivytechapp.permission.MAPS_RECEIVE"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCFO6FZSmyMfeixDkdlheO8dcFpAS6IM9U" />

        <activity
            android:name=".MapsActivity"
            android:label="MapsActivity" />
        <activity
            android:name=".CustomListAdapter.DiningActivity"
            android:label="@string/title_activity_dining"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".WrapNRollScrollingActivity"
            android:label="@string/title_activity_wrap_nroll_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DiningActivityTake2"
            android:label="@string/title_activity_dining_take2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BlueBambooScrollingActivity"
            android:label="@string/title_activity_blue_bamboo_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CampusLinkActivity"
            android:label="@string/title_activity_campus_link"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CalendarLists" />
        <activity android:name=".MapsInterfaceActivity" />
        <activity
            android:name=".Test"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>